<div class="wrapper">
 <div id="logo">
   <h1>Poolite</h1>
 </div>
 <nav>
   <% if @logged %>
    <a href="/">Home</a> <a href="/user/<%=@user.slug%>">Profile</a> <a href="/review/new">Write A Review</a> <a href="/restroom">Restrooms</a>
   <% else %>
     <a href="/">Home</a> <a href="/review/new">Write A Review</a> <a href="/restroom">Restrooms</a>
   <% end %>
 </nav>
</div>
<h1><%= @restroom.restaurant_name%></h1>

<% if !@restroom.reviews.blank? %>
  <% for item in @restroom.reviews %>
    <a href="/review/<%=item.id%>">
      <div class="">
        <%=item.user.username%>
        <h4>
        <%=item.body%>
        </h4>
      </div>
    </a>
  <% end %>
<% else %>
  <h3>This restroom hasn't been reviewed yet! You can be the first to review it <a href="restroom/<%=@restroom.id%>/review">here</a></h3>
<% end %>

<% if @logged %>
  <a href="/logout">Log out</a>
<% end %>
